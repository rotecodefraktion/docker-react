language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t rotecodefraktion/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true rotecodefraktion/docker-react npm run test
